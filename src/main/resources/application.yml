spring:
  application:
    name: resource

  datasource:
    url: jdbc:mysql://localhost:3306/kiritsu_resource?useSSL=false&serverTimezone=UTC
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

